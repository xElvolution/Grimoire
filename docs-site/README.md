# Grimoire documentation

Public docs for [docs.heygrimoire.xyz](https://docs.heygrimoire.xyz).

```bash
npm install
npm run dev      # http://localhost:5173
npm run build
```

Built with VitePress. SDK and CLI are the primary integration surfaces.

Operator setup (server env, keys, deploy) is maintained separately - not in this site.
